<?php
// Placeholder backend for future synchronization/API endpoints.
header('Content-Type: application/json');

$response = [
    'status' => 'ok',
    'message' => 'Backend placeholder ready for future sync features.'
];

echo json_encode($response);
